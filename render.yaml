services:
   - type: web
     name: csv-converter-api
     runtime: node
     rootDir: apps/api
     buildCommand: cd ../.. && pnpm install --frozen-lockfile && pnpm turbo run build --filter=api
     startCommand: node dist/index.cjs
     healthCheckPath: /health
     autoDeploy: true
